NOTE: all collections have automatically assigned _ids (of type ObjectId)

User:
  _id: <ObjectId1>,
  email: string -- probably read about passport before we use this guy
  password: string
end

Classes:
  _id: <ObjectId2> 
  name: string
  user_id: <ObjectId1> (ref: User);
end

Assignment:
  _id: <ObjectId3>
  name: string 
  class_id: <ObjectId2> (ref: Classes)
end

Paper:
  _id: <ObjectId4>
  title_author: string            //For now, a way to identify whose paper it is(concatenate title of paper and last name or something) 
  annotations: [string]
    
  /******************************
  POSSIBLE ALTERNATIVE for annotations:
  annotations: {
    annotation: string,
    citation_id: <ObjectId5> (ref: Citation)
    //This may not work, seems pretty circular. But the annotations will be linked to a particular citation, so maybe
    //annotations will become another Collection.
  }
  *******************************/

  assignment_id: <ObjectId3> (ref: Assignment)
end

Citation:
  _id: <ObjectId5>
  author: string -- family, given, particle
                 -- might have a couple authors
  date: string
  editor: family, given
  container-title: string --?
  edition: string
  volume: string
  pages: string
  type: string
  title: string
  paper_id: <ObjectId4> (ref: Paper);
end

